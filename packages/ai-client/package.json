{
  "name": "@vistafam/ai-client",
  "version": "0.1.0",
  "private": true,
  "description": "VistaFam AI client - Multi-provider LLM abstraction with fallback chains",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "lint": "eslint src --ext .ts",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "openai": "^4.52.0",
    "@anthropic-ai/sdk": "^0.24.0",
    "@google/generative-ai": "^0.14.0",
    "zod": "^3.23.0",
    "dotenv": "^16.4.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.5.0",
    "vitest": "^2.0.0",
    "eslint": "^8.0.0"
  }
}
